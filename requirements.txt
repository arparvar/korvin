# Full Korvin install: dashboard + voice (TTS/STT) out of the box.
# This is what install.sh installs.
#
# Want a lightweight dashboard-only box (no voice)? Install just:
#   pip install -r requirements-base.txt
#
# Pins live in the two files below — edit there, not here.
-r requirements-base.txt
-r requirements-voice.txt
